<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
           
            <link rel="stylesheet" href="{{ asset('web/css/bootstrap.css') }}" type="text/css"/>

            <style>
            .fade.show {opacity: 1;}
            .fade {
                opacity: 0;
                -webkit-transition: opacity 1s linear;
                -o-transition: opacity 1s linear;
                transition: opacity 1s linear;
                transition-property: opacity;
                transition-duration: 1s;
                transition-timing-function: linear;
                transition-delay: initial;
            }
            .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
            .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
            span.incomplete {color: red; font-weight: 700;}
            span.complete {color: green; font-weight: 700;}
            .footer {text-align: center;}
            #taskTable {
                border: 1px solid black;
                {# margin: 5px; #}
                border-collapse: collapse;
            }
            #taskTable td, #taskTable th {
                 border: 1px solid black;
                 padding: 5px 3px;
            }
            #taskTable td.incomplete {
                background: #ff0000;
                color: white;
            }
            #taskTable td.complete {
                background: green;
                color: white;
            }
            </style>
            
        {% endblock %}
    </head>
    <body>
        {% block flash %}{% endblock %}
        {% block body %}{% endblock %}
        <footer>
        {% block footer %}
        {# <div class="footer">
            <a href="{{ path('newtask') }}">New Task</a> | 
            &copy; bretwadleigh.net 2018
        </div> #}
        {% endblock %}
        </footer>
        {% block javascripts %}
        
        <script src="{{ asset('web/js/jquery/jquery.js') }}" type="text/javascript"></script>

        <script src="{{ asset('web/js/bootstrap.js') }}" type="text/javascript"></script>
        
        {% endblock %}
    </body>
        
    
</html>
